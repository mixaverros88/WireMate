/**
 * (Empty file — kept on disk because the sandbox can't delete it.)
 *
 * The real Playwright journeys live in `tests/journeys/`. This file
 * used to be a `test.skip` placeholder; `playwright.config.ts`'s
 * `testMatch: /journeys\/.*\.spec\.ts$/` now filters it out entirely
 * regardless of whether it contains any tests.
 */
export {}
